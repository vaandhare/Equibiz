<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.birdvision.equibiz">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/NoActionBarTheme">
        <activity android:name=".orders.ConfirmOrderActivity" />
        <activity android:name=".orders.OrderActivity" />
        <activity android:name=".notifications.NotificationActivity" />
        <activity android:name=".product.ProductListActivity" />
        <activity android:name=".userInfo.VerificationActivity" />
        <activity android:name=".userInfo.BusinessDetailsActivity" />
        <activity android:name=".product.ProductActivity" />
        <activity
            android:name=".userInfo.UserProfile"
            android:theme="@style/NoActionBarTheme" />
        <activity
            android:name=".userInfo.RegisterActivity"
            android:theme="@style/NoActionBarTheme" />
        <activity
            android:name=".userInfo.LoginActivity"
            android:theme="@style/NoActionBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>